let gameBoard = {
    //make into module
    board: [null, null, null, null, null, null, null, null, null]
}

let displayController = {
     //make into module   
}

let playerFactory = {
    //construct players here
}

let game = {
    //game data goes here
}